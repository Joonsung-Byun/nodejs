<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NodeTrip - My Profile</title>
  <link href="/output.css" rel="stylesheet">
  <link href="/main.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body class="min-h-screen">
  <%- include('nav.ejs') %>

  <div id="mypageWrapper" class="pt-24 pb-16">
    <div class="max-w-6xl mx-auto px-4">
      
      <!-- Profile Header -->
      <div class="card-modern p-8 mb-8">
        <div class="flex flex-col md:flex-row items-center md:items-start gap-8">
          <div class="flex-shrink-0">
            <div class="relative">
              <% if(profileImage == null) {%>
                <div class="w-32 h-32 rounded-full bg-gradient-to-r from-purple-400 to-pink-400 flex items-center justify-center">
                  <span class="text-white font-bold text-4xl"><%= user.username.charAt(0).toUpperCase() %></span>
                </div>
              <% } else { %>
                <img id="userImage" src="<%- profileImage %>" alt="profile" class="w-32 h-32 rounded-full object-cover ring-4 ring-purple-200">
              <% } %>
              
              <!-- Loading Spinner -->
              <div role="status" id="spinner" class="hidden absolute inset-0 flex items-center justify-center bg-black bg-opacity-50 rounded-full">
                <svg class="w-8 h-8 text-white animate-spin" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
              </div>
            </div>
            
            <div class="flex flex-col gap-3 mt-6">
              <button id="imageUploadBtn" class="btn-gradient px-6 py-3 text-center">
                <svg class="w-4 h-4 inline-block mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                Upload Photo
              </button>
              <button id="imageDeleteBtn" class="px-6 py-3 rounded-xl border border-gray-200 text-gray-700 hover:bg-gray-50 transition-all duration-300 text-center">
                <svg class="w-4 h-4 inline-block mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                </svg>
                Remove Photo
              </button>
              <input id="file-input" type="file" class="hidden" accept="image/*"/>
            </div>
          </div>

          <div class="flex-1 text-center md:text-left">
            <h1 class="text-4xl font-bold text-gray-800 mb-2"><%- user.username %></h1>
            <p class="text-gray-600 text-lg mb-6">Travel Enthusiast & Storyteller</p>
            
            <div class="grid grid-cols-3 gap-6 max-w-md mx-auto md:mx-0">
              <div class="text-center">
                <div class="text-2xl font-bold text-purple-600"><%= posts.length %></div>
                <div class="text-gray-500 text-sm">Stories</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-purple-600">0</div>
                <div class="text-gray-500 text-sm">Followers</div>
              </div>
              <div class="text-center">
                <div class="text-2xl font-bold text-purple-600">0</div>
                <div class="text-gray-500 text-sm">Following</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- My Stories Section -->
      <div class="card-modern">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between p-6 border-b border-gray-200">
          <h2 class="text-2xl font-bold text-gray-800 mb-4 sm:mb-0">My Travel Stories</h2>
          <a href="/write" class="btn-gradient px-6 py-3 inline-flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            New Story
          </a>
        </div>

        <div class="p-6">
          <% if(posts.length > 0) { %>
            <div class="space-y-6">
              <% for (let i=0; i < posts.length; i++) { %>
                <article class="bg-gray-50 rounded-2xl p-6 hover:shadow-lg transition-all duration-300" data-post="<%= posts[i].id %>">
                  <div class="flex flex-col lg:flex-row gap-6">
                    <% if (posts[i].thumbnailUrl) { %>
                      <div class="lg:w-48 lg:flex-shrink-0">
                        <img src="<%- posts[i].thumbnailUrl %>" alt="Story cover" class="w-full h-48 lg:h-32 object-cover rounded-xl">
                      </div>
                    <% } %>
                    
                    <div class="flex-1">
                      <div class="flex flex-col h-full">
                        <a href="<%- `/detail/${posts[i].id}` %>" class="group">
                          <h3 class="text-xl font-bold text-gray-800 mb-3 group-hover:text-purple-600 transition-colors duration-300">
                            <%= posts[i].title %>
                          </h3>
                        </a>
                        
                        <div class="singlePost text-gray-600 mb-4 flex-1">
                          <div class="data-content line-clamp-2"><%- posts[i].markdownContent %></div>
                        </div>
                        
                        <% let tagArray = posts[i].tags.split(",") %>
                        <% if (tagArray.length > 0 && tagArray[0] !== "") { %>
                          <div class="flex flex-wrap gap-2 mb-4">
                            <% tagArray.forEach(tag => { %>
                              <span class="px-2 py-1 text-xs bg-purple-100 text-purple-700 rounded-full">
                                #<%= tag.trim() %>
                              </span>
                            <% }); %>
                          </div>
                        <% } %>

                        <div class="flex items-center justify-between">
                          <div class="flex items-center gap-4 text-sm text-gray-500">
                            <span><%- posts[i].createdAt %></span>
                            <span>•</span>
                            <span>0 comments</span>
                            <span>•</span>
                            <span>0 likes</span>
                          </div>

                          <div class="flex items-center gap-3">
                            <a href="<%- `/edit/${posts[i].id}` %>" class="p-2 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-all duration-300" title="Edit story">
                              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                              </svg>
                            </a>
                            <button class="trashcan p-2 text-gray-600 hover:text-red-600 hover:bg-red-50 rounded-lg transition-all duration-300" data-deleteId="<%= `post_id_${posts[i].id}` %>" title="Delete story">
                              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                              </svg>
                            </button>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </article>
              <% } %>
            </div>
          <% } else { %>
            <div class="text-center py-16">
              <svg class="w-16 h-16 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
              </svg>
              <h3 class="text-xl font-semibold text-gray-600 mb-2">No Stories Yet</h3>
              <p class="text-gray-500 mb-6">Start sharing your amazing travel experiences!</p>
              <a href="/write" class="btn-gradient px-8 py-3">Write Your First Story</a>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script src="/mypage.js"></script>
</body>

</html>